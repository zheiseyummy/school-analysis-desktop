# 单机桌面版改造阶段进度

更新时间：2026-09-25（文档交接同步；高中赋分算法暂停）

## 当前工作位置与历史记录校正

继续开发的工程：`E:\项目\school-analysis-desktop`。

接续时发现，工作目录仅留下部分前端，缺少后端、前端构建配置及原记录中的 Git 历史。已从原包恢复缺失文件，保留已有前端改动，重新执行本轮验证。

旧记录中的“406 个 Java 文件编译成功、工作树干净、三个提交已保存”不能用于当前验收。`2a38e39`、`e8c06a6`、`0312bd1` 在此工程不可用。本轮以远程 `master` 的 `232f15f` 为基线，当前本地继续完成分析、备份、综合素质评价、个人导出和成绩导入预览确认；此前默认不自动推送 GitHub，本次用户已明确要求同步完整版本。

- 前端与原 outputs 文档备份：保留在本地审查备份目录，不纳入当前成绩分析系统发布内容。
- 原始前后端源码仍在同一审查目录，用户提供的微信 ZIP 未改动。
- 没有连接、清空或修改用户未授权的真实数据库；本地脱敏样本只用于联调。

## 已完成：外围精简与本机免登录

1. 恢复前后端工程配置、前端依赖锁文件和便携 JDK 17 / Maven 开发工具，不修改全局环境。
2. 保留原 Sidebar、Navbar、Breadcrumb、TagsView、主题、图标和已有业务页面，不重做 UI。
3. 固定菜单：基础信息（年级、班级、学生、教师资料、学科）；成绩管理（考试、成绩录入）；学情分析（年级、班级、学生）。已移除首页，启动后直接进入年级管理。
4. 清理问卷、错题/题库、兴趣爱好、目标院校、监护人、岗位、RBAC、登录和演示模块的外围源码/API，同步清理前端 Mock 与无用依赖。未删除数据库表或数据。
5. 学生/教师不再填写账号密码、重置密码；保留学号/工号、姓名、性别、电话、状态、学生班级年度关系和教师任教关系。学生管理不再展示头像、年龄；教师管理不再展示头像、年龄、出生日期、入职年份、备注。
6. 移除 JWT、Spring Security、Redis、Redisson、验证码、WebSocket、XXL-Job 等依赖链。后端免登录，仅监听 `127.0.0.1:8989`，校验本地 Host/Origin，拒绝跨站请求。
7. 清除运行配置中的原远端地址和凭据；前端开发/预览仅监听本机 3000/4173，端口占用直接报错。
8. 性别维持必填、生日允许空；旧 MySQL 账号密码必填列只作内部兼容，不向 API 暴露。
9. 未分班学生可用独立按钮添加班级，不自动提交空壳关系。更新不传班级列表时保留关系，显式空列表才清除；无效关系应事务回滚。
10. 修正原班级归属保存忽略年度的问题，避免同一学生连续两年同班时覆盖旧记录。
11. 保留字典选项服务；档案源码保留并去掉管理员依赖，暂不加入菜单，后续按学情备注需求改造。

## 本轮验证

| 检查 | 结果与边界 |
| --- | --- |
| TypeScript 和生产构建 | `pnpm typecheck`、`pnpm build:prod` 通过，2531 个模块；Sass 旧 API 弃用警告非构建失败 |
| 静态连线 | `node scripts/check-contracts.mjs` 通过：183 处本地导入、128 个前端 API 适配器、17 个后端控制器，0 错误；不等于全部业务联调 |
| 真实 Chrome 默认业务页 | 通过：免登录打开年级管理，31 个本地资源/API 请求，0 未捕获非 API 异常；后端未启动时记录 2 个预期 API 错误，无远程资源 |
| Vite 开发入口 | 启动成功，仅本机 3000；临时验证服务已停止 |
| 后端编译与测试 | Maven `clean test` 通过（20 项测试、0 失败），`-DskipTests package` 成功；本地 SQLite 服务已启动并完成主要分析、备份和导出接口联调 |
| 后端产物 | `backend/target/school-analysis-desktop-backend.jar`；这是过渡开发 JAR，不是最终桌面 EXE |

可重复执行的脚本在工程 `scripts/`，前端日志和截图在 `.test-results/`，后端测试报告在 `backend/target/surefire-reports/`。原包中依赖外部数据库、硬编码旧路径的 7 组遗留测试已删除；当前测试集只保留本地 SQLite/H2 可重复验证的用例。当前没有完成真实成绩 Excel/PDF 学校样表全链路联调。

离线构建复验耗时 16.156 秒，证明开发依赖缓存完整；本轮清理后运行时已不再包含 MySQL、MinIO 或阿里云 OSS 实现。JAR 不含 Spring Security、Redis、Redisson、Knife4j、XXL、Druid 或 H2 测试依赖。

## 本轮新增：本地文件存储

1. 新增 `LocalFileOssService`，上传文件写入运行目录下的 `files/日期/随机文件名`。
2. 文件返回地址改为本机 `/files/...`，由 Spring 静态资源映射提供预览，不访问 MinIO、阿里云或其他网络对象存储。
3. 删除接口只接受 `/files/` 或 `files/` 相对路径，并使用规范化路径校验，拒绝目录穿越和删除存储目录之外的文件。
4. 开发和生产配置已切换为 `oss.type: local`，MinIO/OSS 远程实现已删除。
5. 前端上传 API 仍保留给 Excel 导入等功能使用；学生和教师管理页面已移除头像上传与预览。

## 本轮新增：SQLite 初始化基础

1. 增加 SQLite JDBC 驱动，默认数据库路径为运行目录下的 `data/school.db`。
2. 启动时自动创建 `data/`、`files/`、`backup/`、`export/` 本地目录。
3. 增加 SQLite 初始化脚本，覆盖年级、班级、学生、教师、学科、考试、成绩、任教关系和档案等核心表，并补充常用索引及外键约束。
4. MyBatis 分页数据库类型切换为 SQLite；H2 测试仍可通过测试配置运行。
5. 已用实际 JAR 启动验证：应用成功创建 `data/school.db` 并建立连接。

当前边界：本地 SQLite 初始化、迁移和活动 Mapper 已完成当前轮清理；原 MySQL 初始化脚本、历史查询脚本、MySQL/MinIO/阿里云 OSS 运行依赖已移除。仍需用学校真实样表联调成绩、综合素质评价和报告，并在 Tauri 阶段验证最终随包运行时。

本轮同时修正了班级关系、任教安排和考试查询中已发现的 SQLite 不兼容聚合/字符串拼接语法，并通过后端 20 项测试。

## 新增分析口径（已确认）

年级分析将增加任课教师视图，按教师、学科、任教班级展示班级横向比较、与年级均分及优秀/及格率差值、历次考试趋势和多班级汇总。班级分析默认比较上一次考试，也支持任意选择两次考试；相对年级进退步按“本班指标变化减去年级指标变化”计算，并扩展差异预警、偏科、分布/箱线图、临界生和进退步榜。

已建立并接入分析接口：年级任课教师班级成绩汇总、班级学科两次考试对比、学生进退步榜、班级学科落后预警、班级偏科分析和 W1～W5/L1～L5 进退分层接口。年级分析页已显示教师班级均分/年级均分/差值和进退步分层，并新增平均分、中位数、标准差、分布、班级与年级差值、学生百分位和优秀/及格临界生；班级分析页已支持任意选择对比考试并显示学科班级变化、年级变化及相对差值；学生分析页已增加考试次数、总分均值/极值、最近变化、班级/年级排名历史和优势/薄弱学科汇总。

当前分析功能状态：年级和班级页面已增加历次考试对比表，展示人数、总分均分、最高分、最低分和较前次均分变化；学生偏科排名差统一按“总分排名 - 学科排名”计算并分格展示；学生个人分析已支持 Excel 导出，包含每场考试、总分、班级/年级排名及各学科成绩和排名。当前本地库包含一个初中年级、一个班级和 44 名学生；成绩考试数据尚未导入，综合素质评价数据单独用于页面验收。

## 本轮新增：数据备份与恢复

已完成本地 SQLite 备份页面和接口：支持立即备份、启动自动备份、最近 30 个备份列表、恢复前确认和恢复后提示。备份文件位于 `backup/`，数据库位于 `data/school.db`。该功能已通过本地接口验证，仍需在无开发环境的 Windows 电脑上做最终验收。

## 初中综合素质评价迁移状态

已根据当前成绩分析系统需求固化六学期、五维度、A/B/C/N/A、累计赋分、班级排名、最终 A/B/C 分档、教师复核锁定和 Excel 导出的业务规则。当前已完成：

1. 综合素质页面改为“初中班级 → 学生名单 → 六学期五维评价”流程，按学号固定匹配学生，姓名仅作为唯一匹配辅助，不再要求手填学生 ID。
2. 增加评价工作簿导入：支持初一上/下、初二上/下、初三上/下及 `junior_*` 工作表；支持单独的“学生名单”工作表；重复导入会更新对应记录，未匹配、重名等情况返回提示供确认。
3. 增加班级名单/评价导出：包含学生名单、五维累计分和六个学期明细工作表。
4. 增加最终评定重新计算、查看和锁定/解锁接口及页面，按班级名次自动生成 A/B/C，并保留有效学期数和缺失学期标记。
5. 班级下拉只查询初中学段，避免高中班级误用该模块。

后端 Maven 编译、前端 `pnpm typecheck` 和生产构建均已通过。最终等级已支持解锁状态下逐项人工调整；当前仍需使用实际样表做一次导入/导出联调，并继续补充学校四页签成品格式和最终 EXE 打包。该模块只对初中学段启用，高中不启用综合素质评价菜单。

## 本轮新增：成绩 Excel 导入预览与确认

1. 成绩导入改为两阶段：先上传并在内存中解析、校验和生成差异预览，确认接口才写入成绩表。
2. 预览会显示总行数、有效/错误行、空白成绩单元格和新增/更新/清除的科目成绩；错误行不会写入。
3. 重新导入时默认保留原成绩，也可以明确选择清除空白单元格的原成绩；空白不会被擅自解释为缺考或正常 0 分。
4. 预览令牌仅在本地内存保留 30 分钟，确认接口使用事务提交，过期或重复确认会被拒绝；重复学生/科目单元格会被标记为错误。
5. 本轮新增接口：`POST /api/v1/exam_body_s/import/preview`、`POST /api/v1/exam_body_s/import/confirm`；原旧导入接口暂保留兼容，前端已切换新流程。

当前边界：考试级配置和导入批次撤销已完成第一版，但学校真实多工作表样表、状态词映射，以及全部分析页面的计总分口径仍需联调；不能宣称已完成正式样表联调。

## 尚未完成与已知风险

## 本轮新增：缺考与未选科状态第一版

1. `sys_score.status` 增加 `NORMAL`、`ABSENT`、`NOT_SELECTED` 三种状态；正常 0 分仍保存为 `NORMAL + score=0`，缺考和未选科保存状态但不写入分数。
2. 成绩 Excel 导入支持在成绩单元格中填写“缺考/缺席/未参加”和“未选科/未选”，并在预览差异中展示新旧状态；无法识别的文字会作为错误行确认，不会写库。
3. 手工成绩录入增加状态选择。选择缺考或未选科后，分数输入框禁用；分析排名、统计和总分 SQL 排除非正常状态，历史无状态且有分数的数据按正常分数兼容。
4. SQLite 启动迁移会为既有 `sys_score` 增加状态列，并将历史有分数记录标记为 `NORMAL`。

当前边界：状态词和空白含义仍需用学校样表确认；按当前需求不主动加入“免考、缓考、作弊、违纪”等其他状态。考试级配置和导入批次撤销已完成第一版。

## 本轮新增：考试级科目与计分配置第一版

1. 考试管理新增“科目设置”，可按场次选择本场考试科目，并逐科设置满分、排序和是否计入总分。
2. 成绩导入预览会按考试配置过滤未选科目，分数范围校验使用考试级满分；没有保存过配置的历史考试继续兼容原有全部科目。
3. 成绩保存时使用考试级满分计算等级；核心总分、排名和学生考试总分查询排除明确设置为“不计入总分”的科目。
4. SQLite 初始化和迁移已增加 `sys_exam_course.count_in_total`，前端类型、接口和文档已同步。

当前边界：配置尚未覆盖全部 Java 分析和正式报告口径；真实学校样表联调、高中选科与赋分仍未完成。

## 本轮新增：成绩导入批次记录与撤销第一版

1. 成绩导入确认后，将每个新增、更新、清除动作写入本地 `sys_score_import_log`，保存导入批次、文件名、学生/科目和导入前后成绩对象快照。
2. 成绩录入页“导入→导入记录”显示最近 30 个批次，支持整批撤销；撤销时先校验当前成绩仍等于导入后的快照，避免覆盖后续人工修改。
3. 新增接口：`GET /api/v1/exam_body_s/import/logs`、`POST /api/v1/exam_body_s/import/undo/{batchId}`。

当前边界：仅支持整批撤销，不支持单条撤销和跨批次合并；旧兼容导入接口仍不写批次日志，前端已使用的新预览确认流程会写入日志。

## 本轮新增：学生学习状态与跟进记录第一版

1. 学生基础资料继续使用原有“备注”字段；学生列表新增“跟进”入口，可查看该学生的历史跟进记录。
2. 跟进记录支持学习状态、特殊情况、跟进内容、下一步计划和跟进日期，支持新增、编辑、删除。
3. 新增本地 SQLite 表 `sys_student_followup` 和接口：`GET/POST /api/v1/student-followups/student/{studentId}`、`PUT/DELETE /api/v1/student-followups/{followupId}`。

当前边界：学习状态选项和特殊情况字段为通用第一版，仍需使用学校真实跟进表确认；暂未加入教师协同、提醒通知和统计报表。

## 本轮新增：学科独立分析与正式 Excel/PDF 报告第一版

1. 新增“学科分析”固定菜单和页面，可按年级或班级、考试、学科查询单科成绩；展示参考人数、有效成绩人数、平均分、中位数、最高/最低分、及格率和优秀率。
2. 学科学生明细支持排名、得分率、状态展示。正常 0 分按正常成绩参与统计；缺考、未选科不参与均值、排名和合格率，但保留在明细中，避免与空白成绩混淆。
3. 新增 `GET /api/v1/analysis/courseAnalysisData`、`GET /api/v1/analysis/courseAnalysisToExcel` 和 `GET /api/v1/analysis/courseAnalysisToPdf`；Excel 报告分为摘要和学生明细，PDF 报告使用横向表格并包含统计摘要。
4. 报告满分优先读取考试级科目配置，未配置时兼容学科默认满分；及格线/优秀线当前按满分的 60%/85% 通用默认值计算，并在学校模板联调时调整。

当前边界：本轮完成的是通用单科报告，不等于学校最终版班级/年级/教师报告模板；所有分析页面对“不计入总分”配置的统一口径仍需继续梳理。

## 本轮新增：高中选科组合与赋分框架第一版

1. 新增“高中选科与赋分”固定菜单，按高中年级维护 3+1+2 选科版本；新版本不会覆盖历史版本。
2. 选科版本支持生效日期、备注和学生组合名单；每名学生保存首选科目、最多两门再选科目、组合编码、来源和备注，历史版本不会被覆盖。
3. 新增接口：`GET/POST /api/v1/student-subject-selections/versions`、`GET/PUT /api/v1/student-subject-selections/versions/{versionId}/records` 及学生选项接口。
4. 成绩模型增加 `scaled_score` 赋分结果字段；考试科目配置增加原始分/赋分结果选择和赋分规则关联。新增赋分规则配置模块，当前 `PENDING` 规则只保存参数，不执行任何未经确认的政策算法。

当前边界：选科名单当前提供页面手工维护，真实选科 Excel 批量导入和历史考试自动绑定仍待补齐；物理类/历史类排名、赋分计算学生群体、青海具体赋分公式和赋分结果回算尚未固化，等待学校规则确认。

## 本次联调：综合素质评价真实样表

已使用用户提供的 `6班.xlsx` 做本地导入验证：工作簿包含七上、七下、八上、八下、九上、九下 6 个学期工作表，5 个评价维度；共识别 44 名学生、258 行有效记录，按学号全部匹配，无未匹配提示。已在本机初中示例班级生成综合素质评价累计结果 220 条，并启动前端 `http://127.0.0.1:3000/#/quality/evaluation` 与后端 `http://127.0.0.1:8989` 供页面核对。

本次导入数据只写入本机被 `.gitignore` 忽略的 SQLite 数据库，不复制到仓库、不提交 GitHub；学生姓名和评价明细不写入文档。表格中只有部分学生出现在全部六个学期，系统会按实际记录显示有效学期数和缺失学期标记。

本地演示数据联调时发现，旧考试记录的日期字段可能只有 `yyyy-MM-dd`，而后端实体按日期时间读取会导致年级分析请求失败。已兼容空学期/未知考试类型，并将演示考试日期规范化为完整日期时间；年级分析、教师分析、成绩诊断和学生成绩接口已用两场演示考试重新验证通过。

本轮分析页面调整：学生偏科排名差统一为“总分排名 - 学科排名”，并用分格标签展示各学科差值；年级和班级分析新增历次考试对比表，展示人数、总分均分、最高分、最低分及较前次均分变化；两页增加分隔标题，降低成绩明细、统计图表和分析结果的视觉拥挤。

同时修正班级分析的班级选择：选择带年级的班级后自动补齐年级 ID，确保班级学科与年级对比接口可以正常查询。

## 本轮新增：综合素质评价缺失确认与最后学期名单

1. 综合素质评价页面完成工作台式布局美化，新增最终统计人数、已生成等级、待确认缺失和班级人数摘要。
2. 导入包含最后一个学期（九下）工作表时，系统将该学期出现的学生固化为最终评价范围；重新生成最终评定只按该范围统计，不再按当前班级关系表全部人数统计。
3. 对最终评价范围学生逐学期检查五个维度，缺少记录或出现 N/A 时生成待确认项；支持“确认转入”和“确认缺失”，未确认学生不会直接获得最终等级。
4. 最终评定页增加缺失风险提示、有效学期和数据状态；新增缺失确认接口与本地 SQLite 表，重复导入不会覆盖已确认状态。

本次 `6班.xlsx` 本地复核结果：最终统计范围 44 人，其中 3 名学生、6 条学期缺失记录待确认；未确认前已生成 41 人的最终等级（205 条维度结果）。数据仍只保存在本机 SQLite，未上传 GitHub。

正式结果导出规则：最终结果表首列为姓名，随后为思想品德、学业水平、身心健康、艺术素养、实践与创新五个最终分值，再列出对应五个最终等级和学号；A/B/C 比例可按班级设置，保存后重新计算自动等级。老师可逐项调整最终等级，必须处理缺失项并锁定确认后才能导出正式结果。

个人“当前已保存记录”调整为六学期纵向展示：学期固定在最左侧，五个维度横向排列，各学期依次展示已保存等级；上方的评价编辑矩阵保持原有录入布局。

学生个人分析新增 Excel 导出，可导出学号、姓名、每场考试信息、总分、班级/年级排名以及各学科成绩和学科排名；已用演示学生实际请求验证文件下载成功。

- MySQL、MinIO 和阿里云 OSS 运行依赖已从 POM 和本地配置清理；文件服务仅保留本地实现，当前仍不是最终绿色 EXE。
- SQLite 已完成初始化基础，备份恢复后端和前端入口已完成；活动 Mapper 已按 SQLite 口径检查，原始 SQL 已移除，尚未形成最终绿色 EXE。
- 考试级配置、缺考/未选科状态和新导入流程的批次撤销已具备第一版；计分快照、旧导入接口日志和真实样表口径仍未形成完整闭环。
- 原删除学生/教师/班级逻辑对历史成绩、任教关系和档案的保护不足；迁移时需停用/归档、引用检查和备份，暂不建议本阶段处理真实数据。
- 原导入模板和列映射仍需统一。补充文档提及的学校 16 页签样表本轮未在工作区取得，不宣称已对其完成实测。
- 赋分群体、个人报告模板等待定项没有擅自实现；高中赋分算法按当前要求暂停，保留配置框架。
- 当前品牌名称为“成绩分析系统”，不代表已覆盖完整初高中场景。

## 本次完整检查与清理（2026-09-25）

1. 前端 `pnpm typecheck`、`pnpm build:prod` 通过；生产构建转换 2535 个模块。浏览器预览烟测通过：默认业务页 48 个本地请求、0 个 API 错误、0 个浏览器异常；综合素质评价页实际加载班级和学生数据，0 个浏览器异常。
2. 后端项目内置 JDK 17 + Maven `test` 通过：20 项测试、0 失败、0 错误。测试日志中的参数校验异常是专门验证回滚/拦截行为的预期输出。
3. `node scripts/check-contracts.mjs` 通过：183 处本地导入、128 个 API 适配器、17 个控制器，0 错误。
4. 对 `backend/data/school.db` 做只读关联审计：44 名学生、44 条班级关系、1290 条综合素质记录、44 条最终名单范围；学生/班级/年级/评价记录无孤儿、无重复键。当前考试、学科、成绩均为 0 条，因此没有把空库误报为“成绩链路已联调”。
5. 在临时副本中写入一条虚拟“年级→班级→学生→考试→科目→成绩”链路，关联查询和计入总分查询均通过；临时副本已自动清理，不改动本地正式库。
6. 清理没有运行时引用的历史借用材料：删除 `backend/doc` 下的需求 Word、截图和图片压缩包、上游 CHANGELOG/Docker 部署文件、依赖外部环境的遗留测试，以及未被代码引用的 `用户导入模板.xlsx`；保留成绩、学生、教师模板和运行必需文件。
7. 本地演示库残留的旧年级显示名称已改为通用“初中”，学生、班级、评价记录及学号均未改变；重新请求班级接口返回的名称已不再包含旧项目名称。

## 本次管理字段精简（2026-09-25）

1. 学生管理列表和编辑表单移除头像、年龄；学生学号、姓名、性别、出生日期、入学年份、班级、电话、备注和状态继续保留。
2. 教师管理列表、筛选、编辑表单和导出移除头像、年龄、出生日期、入职年份、备注；保留工号、姓名、性别、电话和状态。
3. 后端数据库兼容字段暂不删除，避免影响已有数据和迁移；列表接口不再计算年龄，教师导出不再输出已移除列。
4. 前端管理页实际浏览器检查通过，学生和教师页面均无运行时异常，已移除字段不再出现在页面文本中。

## 下一步实施顺序

1. 用学校实际成绩和选科样表联调导入、重导确认、缺考/未选科、选科版本和考试科目配置，并统一全部分析/导出计总分口径。
2. 根据学校确认的赋分政策补齐计算群体、规则参数、原始分/赋分结果计算及物理类/历史类排名。
3. 使用学校实际样表联调成绩、综合素质评价、选科名单和跟进记录，并继续核对 SQLite 迁移边界。
4. 结合学校实际跟进表细化学生跟进字段，并将学科/班级/年级/教师报告调整为正式模板和批量导出。
5. 使用综合素质评价实际样表完成四页签成品格式联调。
6. 接入 Tauri，生成 Windows EXE 和绿色目录，在没有开发环境的电脑上离线验收。
